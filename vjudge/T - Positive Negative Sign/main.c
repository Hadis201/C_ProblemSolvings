#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b, n;
    scanf("%d", &n);

    for(int i = 0; i<n; i++){
        scanf("%d %d", &a, &b);
        int y = n*m/2;
        printf("Case %d: %d", i + 1, y);

    }
    int ans =

    return 0;
}
